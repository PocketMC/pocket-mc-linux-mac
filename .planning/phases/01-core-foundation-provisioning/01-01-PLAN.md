---
phase: 01-core-foundation-provisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PocketMC.App/Program.cs
  - PocketMC.Core/Services/ISecretStore.cs
  - PocketMC.Core/Services/ISettingsService.cs
  - PocketMC.Core/Models/Settings.cs
  - PocketMC.Infrastructure/Services/SettingsService.cs
  - PocketMC.Platform/Services/MacKeychainSecretStore.cs
  - PocketMC.Platform/Services/LinuxSecretServiceSecretStore.cs
  - PocketMC.Platform/Services/AesFallbackSecretStore.cs
  - PocketMC.Platform/Services/SecretStoreFactory.cs
autonomous: true
requirements:
  - HOST-01
  - HOST-02
  - HOST-03
  - HOST-04

# Goal-backward verification
must_haves:
  truths:
    - "Application initializes dependency injection container on boot."
    - "Configuration directory structure is created under system-default paths."
    - "Secrets are encrypted using AES fallback when Keychain/libsecret is unavailable."
    - "Data path overrides redirect data directories to custom locations while keeping settings.json in place."
  artifacts:
    - path: "PocketMC.Core/Services/ISecretStore.cs"
      provides: "Secure storage abstraction"
    - path: "PocketMC.Infrastructure/Services/SettingsService.cs"
      provides: "Settings manager with path override logic"
    - path: "PocketMC.Platform/Services/AesFallbackSecretStore.cs"
      provides: "PBKDF2 machine-keyed AES encryption"
  key_links:
    - from: "PocketMC.Infrastructure/Services/SettingsService.cs"
      to: "PocketMC.Core/Models/Settings.cs"
      via: "Binds JSON file to configuration model"
---

<objective>
Implement the dependency-injected hosting core, storage folders layout initialization, and the cross-platform secure store service with Keychain, libsecret, and PBKDF2-AES fallbacks.

Purpose: Form the core system environment and configuration layer needed by all other server management services.
Output: DI setup, settings serializer, system folder creator, and native OS secret store clients.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-foundation-provisioning/01-CONTEXT.md
@.planning/phases/01-core-foundation-provisioning/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap DI Host & Directory Layout</name>
  <files>PocketMC.App/Program.cs, PocketMC.Core/Models/Settings.cs, PocketMC.Core/Services/ISettingsService.cs, PocketMC.Infrastructure/Services/SettingsService.cs</files>
  <read_first>.planning/phases/01-core-foundation-provisioning/01-CONTEXT.md</read_first>
  <action>
    Create the Core Settings model containing fields for version, customDataRoot, and downloadedRuntimes (Java and PHP maps). 
    Implement SettingsService using System.Text.Json. On startup, initialize default paths:
      - Linux: ~/.config/PocketMC/
      - macOS: ~/Library/Application Support/PocketMC/
    Create subfolders: Instances/, Backups/, Downloads/, Cache/, Logs/ relative to the config root (or customDataRoot if set in settings.json). This implements configurable custom root path support (D-03).
    Configure Microsoft.Extensions.Hosting HostBuilder in Program.cs, registering SettingsService as a Singleton.
  </action>
  <verify>dotnet build passes</verify>
  <acceptance_criteria>
    - SettingsService.cs contains paths for standard Linux and macOS app support directories.
    - SettingsService.cs creates Instances, Backups, Downloads, Cache, and Logs directories.
    - Program.cs uses Host.CreateDefaultBuilder or HostApplicationBuilder to resolve settings services.
  </acceptance_criteria>
  <done>Core host boots and sets up default folders successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Build Cross-Platform Secret Store Services</name>
  <files>PocketMC.Core/Services/ISecretStore.cs, PocketMC.Platform/Services/MacKeychainSecretStore.cs, PocketMC.Platform/Services/LinuxSecretServiceSecretStore.cs, PocketMC.Platform/Services/AesFallbackSecretStore.cs, PocketMC.Platform/Services/SecretStoreFactory.cs</files>
  <read_first>.planning/phases/01-core-foundation-provisioning/01-RESEARCH.md</read_first>
  <action>
    Create ISecretStore interface with GetAsync, SetAsync, and DeleteAsync string options.
    Implement MacKeychainSecretStore calling Security.framework via DllImport for Keychain storage.
    Implement LinuxSecretServiceSecretStore calling libsecret-1.so.0 via DllImport for Secret Service.
    Implement AesFallbackSecretStore deriving an encryption key from machine MAC address and hardware UUID (reading /etc/machine-id on Linux or ioreg UUID on macOS) using PBKDF2 (Rfc2898DeriveBytes), encrypting values via AesGcm. This implements the silent AES encryption fallback policy (D-01).
    Create SecretStoreFactory that checks OS platform, probes native keyring libraries, catches P/Invoke exceptions, and falls back to AesFallbackSecretStore. Register the resolved store in DI.
  </action>
  <verify>dotnet test passes unit tests checking storage fallbacks</verify>
  <acceptance_criteria>
    - ISecretStore.cs defines methods for credential storage.
    - AesFallbackSecretStore.cs uses AesGcm or AesManaged to encrypt settings values.
    - SecretStoreFactory.cs instantiates AesFallbackSecretStore when platform libraries fail to load.
  </acceptance_criteria>
  <done>Secure storage service resolves platform keychains with AES fallback.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run xunit unit tests for SettingsService custom roots and validation.
- [ ] Run secret store mock tests ensuring AES fallback works under simulation.
</verification>

<success_criteria>
- DI host runs successfully.
- System directories created on startup.
- Secure settings fallback securely encrypts text settings silently.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation-provisioning/01-01-SUMMARY.md`
</output>
