---
phase: 01-core-foundation-provisioning
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - PocketMC.Core/Services/IJavaService.cs
  - PocketMC.Core/Services/IPHPService.cs
  - PocketMC.Infrastructure/Services/JavaService.cs
  - PocketMC.Infrastructure/Services/PHPService.cs
  - PocketMC.Infrastructure/Utils/SafeZipExtractor.cs
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
  - SEC-01

# Goal-backward verification
must_haves:
  truths:
    - "Adoptium v3 API downloads JDK 8/11/17/21/25 runtimes."
    - "PocketMine PHP binaries are extracted and permissions set programmatically."
    - "Zip Slip traversal attempts are blocked and log errors."
    - "Validates java -version and php -v before launching."
  artifacts:
    - path: "PocketMC.Infrastructure/Utils/SafeZipExtractor.cs"
      provides: "Zip Slip protection logic"
    - path: "PocketMC.Infrastructure/Services/JavaService.cs"
      provides: "Temurin API download and installer"
    - path: "PocketMC.Infrastructure/Services/PHPService.cs"
      provides: "PMMP binary extract and check"
  key_links:
    - from: "PocketMC.Infrastructure/Services/JavaService.cs"
      to: "PocketMC.Infrastructure/Utils/SafeZipExtractor.cs"
      via: "Decompresses JDK files safely"
---

<objective>
Implement the Safe Zip/Tar Extractor, Java provisioner (Temurin), PHP provisioner (PocketMine), and pre-launch binary verifiers.

Purpose: Automate the retrieval and validation of correct Java and PHP environment runtimes on Linux and macOS.
Output: Extract tools, runtime managers, background download pipelines, and execution verifiers.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-foundation-provisioning/01-CONTEXT.md
@.planning/phases/01-core-foundation-provisioning/01-RESEARCH.md
@.planning/phases/01-core-foundation-provisioning/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Safe Archive Extractor (SafeZipExtractor)</name>
  <files>PocketMC.Infrastructure/Utils/SafeZipExtractor.cs</files>
  <read_first>.planning/phases/01-core-foundation-provisioning/01-RESEARCH.md</read_first>
  <action>
    Implement SafeZipExtractor containing ExtractZip and ExtractTarGz methods.
    Include validation to prevent path traversal vulnerability (Zip Slip). For each entry in the archive, verify that the resolved extraction path lies strictly within the target directory boundary using:
      Path.GetFullPath(destinationPath).StartsWith(Path.GetFullPath(targetDir) + Path.DirectorySeparatorChar)
    Throw an exception if a traversal attempt is detected. Preserve UNIX file permission bits during tar/zip extraction.
  </action>
  <verify>dotnet build passes</verify>
  <acceptance_criteria>
    - SafeZipExtractor.cs has code checking resolved file paths against destination directory limits.
    - SafeZipExtractor.cs throws exception on invalid path segments (e.g., "../").
  </acceptance_criteria>
  <done>Archive extractor blocks path traversal attempts successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Java & PHP Provisioner Services</name>
  <files>PocketMC.Core/Services/IJavaService.cs, PocketMC.Core/Services/IPHPService.cs, PocketMC.Infrastructure/Services/JavaService.cs, PocketMC.Infrastructure/Services/PHPService.cs</files>
  <read_first>.planning/phases/01-core-foundation-provisioning/01-RESEARCH.md</read_first>
  <action>
    Implement IJavaService and IPHPService interfaces.
    Implement JavaService using HttpClient to query Adoptium API v3 redirects, download the JDK archive into Cache/Downloads, verify SHA256 checksum, and extract it using SafeZipExtractor into local data folders. Downloads are triggered on-demand (D-02).
    Implement PHPService to query PMMP releases, download the selected PHP binary archive, extract, and explicitly call `chmod +x` via a process execution wrapper on the extracted php binary.
    Implement pre-launch validation in both services by starting the binaries with `-version` / `-v` flags, reading standard output/error, and parsing the version string. If validation fails, block launch and prepare error reporting info (D-04).
  </action>
  <verify>dotnet test passes for download extraction and validation</verify>
  <acceptance_criteria>
    - JavaService.cs calls api.adoptium.net v3 endpoints.
    - PHPService.cs runs chmod +x on the php binary on Unix hosts.
    - Verification methods execute local processes to parse output.
  </acceptance_criteria>
  <done>Runtimes download, extract safely, and version validation checks pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run xunit unit tests for SafeZipExtractor traversal prevention.
- [ ] Run mock integration tests verifying Java and PHP download managers query URLs correctly.
</verification>

<success_criteria>
- Zip Slip traversal attempts are correctly blocked.
- Java Temurin packages download and extract.
- PHP binaries extract and can be executed natively.
- Pre-launch checks successfully verify runtime outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation-provisioning/01-02-SUMMARY.md`
</output>
